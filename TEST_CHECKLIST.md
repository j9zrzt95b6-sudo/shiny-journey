# Smart Care 驗收清單（可點擊測試）

## A. 進入與導航
- [ ] 可開啟首頁與側邊欄各頁（首頁、個案、每日紀錄、代幣、ISP、分析、報告、設定、操作紀錄、手機預覽）
- [ ] 側邊欄切頁後不會白屏，按鈕可重複切換

## B. 分析中心（角色層級）
- [ ] 角色選 `管理員` 時可選 `全機構 / 班級 / 個人`
- [ ] 角色選 `教保員` 時僅可選 `班級 / 個人`
- [ ] 各圖卡都可用日期、班級、個案、工作項目篩選
- [ ] 日期切到 `自訂` 時可輸入起訖日
- [ ] 點代幣趨勢圖某一欄會跳出該期間工作代幣明細

## C. 報告收集區
- [ ] 右側有固定 `報告收集區`
- [ ] 可按 `加入目前圖表（PNG）`
- [ ] 可按「圖表類/多媒體類」加入項目
- [ ] 可按 `加入 AI 成果摘要`
- [ ] 每個已加入項目可按 `移除`
- [ ] 可拖曳排序（改變項目先後）
- [ ] 可按 `清空`

## D. 一鍵報告
- [ ] `一鍵建立 ISP 報告` 可自動加入多個項目
- [ ] `主管模式班級報告` 可自動建立班級版內容
- [ ] 報告預覽區會依收集區順序顯示頁面

## E. 匯出
- [ ] `匯出 Excel` 可下載，中文欄位正常
- [ ] `產生 PDF` 可下載
- [ ] PDF 頁序與報告收集區拖曳排序一致

## F. 基本 CRUD
- [ ] 個案可新增 / 管理載入 / 更新 / 刪除
- [ ] 每日紀錄可新增 / 更新 / 刪除
- [ ] ISP 可新增 / 更新 / 刪除
- [ ] 班級可新增 / 改名 / 刪除
- [ ] 教保員可新增 / 更新 / 刪除
- [ ] 工作項目可新增 / 更新 / 刪除

## G. 操作紀錄
- [ ] 執行新增、更新、刪除、匯出後，操作紀錄頁有新增紀錄

## H. 回歸重點
- [ ] `管理 / 查看 / 預覽` 按鍵都有反應
- [ ] 班級、個案切換後圖表有同步更新
- [ ] 報告頁重整後仍可正常重新加入項目（前端暫存版）

## I. 同步驗收（本次重點）
- [ ] 工具列可看到 `立即同步` 按鈕與同步狀態文字
- [ ] 按 `立即同步` 後，顯示 `雲端同步完成` 並更新時間
- [ ] A 裝置新增一筆資料後，B 裝置按 `立即同步` 可看到同筆資料
- [ ] B 裝置修改同筆資料後，A 裝置重新整理或回到分頁可看到更新
- [ ] 斷網後恢復網路，頁面狀態可自動切回 `已同步`（或手動按一次 `立即同步`）
- [ ] 重新整理頁面後，資料仍為最新累積結果（非回到舊版）

## J. 班級鎖定模式逐步驗收（新增）

### 測試前置
- [ ] 角色設為 `管理員` 或 `督導`
- [ ] 至少建立 2 個班級，且每個班級至少 1 位個案
- [ ] 由 `設定` 頁確認可看到班級鎖定開關與鎖定班級選擇

### 腳本步驟（每一步都要核對預期）
1. 在 `設定` 頁開啟班級鎖定，選定 A 班。
	預期：上方出現鎖定提示條，文字顯示 A 班。
2. 看側欄 `首頁` 按鈕。
	預期：出現紅色 `鎖` 提示。
3. 切到 `首頁`。
	預期：`個案` 指標只計算目前工具列範圍（A 班 / 指定個案），不應是全機構總數。
4. 切到 `代幣`。
	預期：個案名單、月統計、兌換紀錄都只出現 A 班範圍資料。
5. 在 `代幣` 的兌換紀錄篩選選 `本月`。
	預期：僅顯示本月資料，不可混入其他月份資料。
6. 切到 `分析`。
	預期：班級下拉為禁用狀態（灰化/鎖定色），且欄位下方顯示「鎖定模式中：請先解除鎖定，才能切換班級」。
7. 在 `分析` 嘗試切換個案與日期。
	預期：可切換個案與日期，但資料仍受 A 班範圍限制。
8. 切到 `報告`。
	預期：`班級（以上方篩選為準）` 與 `個案（以上方篩選為準）` 欄位跟隨工具列，不可手動改成跨班。
9. 使用工具列 `快速解除鎖定`（若顯示）。
	預期：鎖定提示條消失、首頁紅色 `鎖` 消失、分析班級下拉恢復可選。
10. 再次切回 `代幣` 與 `分析`。
	 預期：可看到全班級選項，且資料範圍可隨班級切換。

### 通過判定
- [ ] 上方篩選、分析中心、報告頁三者班級/個案範圍一致
- [ ] 鎖定啟用時不會看到其他班級個案的當月兌換統計
- [ ] 鎖定解除後，所有受限 UI 能即時恢復

### 測試紀錄表模板（可複製填寫）

#### 1) 測試基本資料
| 欄位 | 內容 |
|---|---|
| 測試日期 | YYYY-MM-DD |
| 測試人員 |  |
| 測試環境 | index.html / official.html |
| 裝置 | Desktop / Mobile |
| 瀏覽器 | Chrome / Edge / Safari |
| 版本註記 | 例如：鎖定提示條 + 側欄紅鎖 + 分析班級禁用 |

#### 2) 步驟驗收紀錄
| 步驟編號 | 測試動作 | 預期結果 | 實際結果 | 判定 (Pass/Fail) | 截圖路徑 | 備註 |
|---|---|---|---|---|---|---|
| 1 | 啟用班級鎖定並選 A 班 | 出現鎖定提示條且顯示 A 班 |  |  |  |  |
| 2 | 檢查側欄首頁按鈕 | 出現紅色鎖提示 |  |  |  |  |
| 3 | 切到首頁看個案指標 | 只顯示目前範圍個案數 |  |  |  |  |
| 4 | 切到代幣看名單/統計/紀錄 | 僅 A 班資料 |  |  |  |  |
| 5 | 代幣篩選選本月 | 只顯示本月資料 |  |  |  |  |
| 6 | 切到分析檢查班級欄位 | 班級欄位禁用且有鎖定文案 |  |  |  |  |
| 7 | 分析切換個案與日期 | 可切換但仍限制 A 班範圍 |  |  |  |  |
| 8 | 切到報告檢查班級/個案欄位 | 跟隨工具列且不可跨班 |  |  |  |  |
| 9 | 使用快速解除鎖定 | 鎖定提示條與紅鎖消失 |  |  |  |  |
| 10 | 再回代幣與分析 | 班級可自由切換 |  |  |  |  |

#### 3) 缺陷紀錄（若有）
| 缺陷編號 | 發生步驟 | 現象 | 重現步驟 | 嚴重度 (High/Medium/Low) | 狀態 |
|---|---|---|---|---|---|
| BUG-001 |  |  |  |  | Open |

#### 4) 最終結論
- [ ] 本輪驗收通過，可上線
- [ ] 本輪驗收不通過，需修正後複測
- 結論說明：

## 目前版本限制（預期行為）
- 本版資料採 `localStorage + Cloudflare Workers` 同步；同站台跨裝置可共用最新累積資料
- 若 Workers 無法連線，仍可在單裝置使用 `localStorage`（跨裝置同步會暫停）
- 多媒體目前為流程展示，尚未接真實檔案上傳與檔案儲存服務
