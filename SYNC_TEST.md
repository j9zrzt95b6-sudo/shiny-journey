# 跨裝置同步測試指南

## 快速開始

### 先確認後端健康狀態（建議先做）

```bash
./scripts/deploy-cloudflare.sh
./scripts/cloudflare-healthcheck.sh
```

若顯示缺少憑證，先建立：

```bash
cp .env.cloudflare.example .env.cloudflare
# 編輯 .env.cloudflare，填入 CLOUDFLARE_API_TOKEN 與 CLOUDFLARE_ACCOUNT_ID
```

### 場景 A：電腦端新增資料 → 手機端同步驗證

#### 第一步：設定同步碼
1. **電腦**：打開設定 → 跨裝置同步連結
2. 確認同步碼（例如 `test-sync-2026-07-26`）
3. 確認同步 API 位址已填入（Cloudflare Workers 或 Netlify Functions）
4. **手機**：在地址欄輸入電腦複製的同步連結，或手動填入同步碼+API位址

#### 第二步：電腦端添加資料
1. **電腦**：切換到「個案」頁籤
2. 點擊「新增個案」，填入以下內容：
   - 名稱：`測試個案_` + 時間戳（例如 `測試個案_202607261530`）
   - 班級：選擇任意班級
   - 其他欄位隨意填寫
3. 點擊「保存」
4. **工具列**查看同步狀態 → 確認顯示「已同步（時間）」

#### 第三步：手機端驗證
1. **手機**：按下「立即同步」按鈕
2. 等待狀態文字顯示「已同步」
3. 切換到「個案」頁籤
4. **驗證**：能否看到電腦剛才新增的個案？
   - ✅ 能看到 → **同步正常**
   - ❌ 看不到 → **同步異常**（詳見故障排除）

---

### 場景 B：電腦端修改資料 → 手機端同步驗證

#### 前提條件
- 已完成場景 A 的同步
- 手機端已看到電腦新增的個案

#### 第一步：電腦端修改資料
1. **電腦**：個案頁籤中找到剛才新增的個案
2. 點擊「編輯」
3. 修改「備註」欄位，例如：`修改於電腦_` + 時間戳
4. 點擊「保存」
5. **工具列**確認同步狀態已更新

#### 第二步：手機端驗證
1. **手機**：按下「立即同步」按鈕
2. 切換到「個案」頁籤（或重新整理）
3. **驗證**：個案備註是否已更新？
   - ✅ 備註已更新 → **同步正常**
   - ❌ 備註未更新或還是舊值 → **同步異常**

---

### 場景 C：版本衝突檢驗

#### 第一步：製造版本衝突（需要斷網模擬）
1. **電腦**：進入設定，複製當前的「同步碼」
2. **手機**：打開開發者工具，進入應用頁籤
   - 找到 `smart-care-state-v1` 的 localStorage
3. **電腦端做修改**（例如修改個案名稱）
4. **手機同時做修改**（例如修改同一個個案的班級）
5. 雙方都分別點「立即同步」
6. **觀察結果**：
   - 服務端應返回 409 衝突
   - 衝突方應自動拉取最新的雲端資料
   - 查看同步狀態文字

---

### 場景 D：網路恢復後自動同步

#### 第一步：模擬網路中斷
1. **任意裝置**：修改資料後斷網（關閉 WiFi 或使用開發者工具 DevTools → Network → Offline）
2. 此時同步狀態應顯示「讀取失敗」或「寫入失敗」

#### 第二步：恢復網路
1. 重新開啟網路連線
2. **觀察**：頁面是否自動調用 `reconcileCloudState("網路恢復")`？
3. 同步狀態應自動更新為「已同步」

---

## 故障排除

### 問題 1：手機端按「立即同步」後無反應
**可能原因**：
- API 位址未正確填入（檢查設定 → 同步 API 位址）
- API 服務未啟動或無法連線（檢查 Cloudflare Workers/Netlify 狀態）
- 同步碼不正確（手動重新貼上）

**快速確認**：
- 先在專案根目錄執行 `./scripts/cloudflare-healthcheck.sh`
- 若失敗，先修復部署/憑證，再回到前端同步測試

**排查步驟**：
1. 打開瀏覽器開發者工具（F12）
2. Network 頁籤觀察請求
3. 查看發送給 API 的 URL 和回應
4. 檢查 Console 有無錯誤訊息

### 問題 2：資料在某個裝置更新後，另一裝置看不到
**可能原因**：
- localStorage 版本訊息不匹配
- `_meta.baseUpdatedAt` 或 `_meta.lastSyncedAt` 未正確設定

**排查步驟**：
1. 開啟開發者工具 → Application/Storage → LocalStorage
2. 檢查 `smart-care-state-v1` 的內容
3. 展開 `_meta` 物件，驗證：
   - `updatedAt`：最後更新的時間戳
   - `lastSyncedAt`：最後成功同步的時間戳
   - `baseUpdatedAt`：客戶端本地版本號（用於衝突檢驗）

### 問題 3：修改後長時間未同步
**可能原因**：
- 自動同步延遲（預設 20 秒檢查一次）
- 用戶正在輸入中（系統會延遲同步避免打斷編輯）

**解決方式**：
- 等待 20 秒或按「立即同步」按鈕手動觸發
- 確保未在輸入框中編輯

---

## 驗收標準

- ✅ 場景 A：電腦新增 → 手機能看到
- ✅ 場景 B：電腦修改 → 手機能看到更新
- ✅ 場景 C：衝突時自動選用較新版本或合併
- ✅ 場景 D：網路恢復後自動同步
- ✅ 同步狀態文字正確反映當前狀態

---

## 測試資料快速清理

如需清理測試資料：
1. **本機 LocalStorage**：設定 → 資料維護 → 「清除範例資料」
2. **雲端資料**：同時清除 Cloudflare KV/Netlify Blobs 中的對應同步碼資料
3. 建議保留一個穩定的同步碼用於長期測試

---

## 開發者工具輔助指令

### 在瀏覽器 Console 執行以查看當前同步狀態

```javascript
// 查看本機狀態
console.log("本機同步狀態：", {
  cloudSyncKey,
  cloudSyncBaseUrl,
  cloudHydrationReady,
  lastSyncedAt: getLastSyncedAt(),
  hasPending: hasPendingLocalChanges()
});

// 手動觸發同步
manualCloudSync();

// 查看 localStorage 內容
console.log(JSON.parse(localStorage.getItem("smart-care-state-v1")));

// 查看 meta 資訊
const state = JSON.parse(localStorage.getItem("smart-care-state-v1"));
console.log("Meta 資訊：", state._meta);
```

---

## 常見測試組合

### 最小化測試流程（15 分鐘）
1. 電腦 + 手機，同一個 WiFi
2. 設定相同同步碼
3. 電腦新增一筆個案 → 手機同步驗證
4. 電腦修改該個案 → 手機同步驗證

### 完整測試流程（1 小時）
1. 完成最小化流程
2. 新增多筆資料（10+ 筆）
3. 多次修改和刪除
4. 模擬網路中斷和恢復
5. 刷新頁面驗證資料持久性

