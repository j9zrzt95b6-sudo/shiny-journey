🚀 立即開始跨裝置同步驗證
===========================

## ⏱️ 快速開始（3 分鐘）

### 準備工具
- [ ] 電腦瀏覽器（Chrome/Firefox/Safari）
- [ ] 手機瀏覽器
- [ ] 相同 WiFi 網絡（或手機可訪問的網絡）

### 先確認後端已就緒（建議）
```bash
./scripts/deploy-cloudflare.sh
./scripts/cloudflare-healthcheck.sh
```

若第一次使用請先建立憑證檔：
```bash
cp .env.cloudflare.example .env.cloudflare
# 編輯 .env.cloudflare，填入 CLOUDFLARE_API_TOKEN 與 CLOUDFLARE_ACCOUNT_ID
```

---

## 🎯 方案 1：生產環境驗證（推薦）

### 步驟 1：電腦端打開應用
```
複製貼上這個地址到電腦瀏覽器：
https://j9zrzt95b6-sudo.github.io/shiny-journey/?syncKey=center-a

✅ 頁面應加載完成
✅ 工具列應顯示同步碼 center-a
```

### 步驟 2：電腦端新增個案
```
1. 切換到「個案」頁籤
2. 點「新增個案」
3. 填入信息（例如：姓名「李小明」、班級「生訓1班」）
4. 點「保存」
5. 在工具列點「立即同步」
6. 應顯示「已同步」和時間戳
```

### 步驟 3：手機端打開應用
```
複製貼上到手機瀏覽器：
https://j9zrzt95b6-sudo.github.io/shiny-journey/?syncKey=center-a

✅ 應自動使用相同同步碼
```

### 步驟 4：手機端同步驗證
```
1. 在手機工具列點「立即同步」
2. 等待顯示「已同步」
3. 切換到「個案」頁籤
4. 查看是否看到電腦新增的「李小明」？

✅ 能看到 → 同步成功！
❌ 看不到 → 見下方「故障排除」
```

---

## 🎯 方案 2：本地測試工具驗證（詳細診斷）

### 步驟 1：電腦端打開測試工具
```
http://localhost:8000/sync-test.html

✅ 應看到「同步測試工具」介面
```

### 步驟 2：配置同步碼
```
電腦端：
• 同步碼：center-a
• API：https://smart-care-sync-api.j9zrzt95b6.workers.dev/state
• 點「保存設定」
```

### 步驟 3：電腦端執行操作
```
1. 切換到 💻 電腦端（應已選中）
2. 在「測試資料描述」輸入：李小明
3. 選擇「新增個案」
4. 點「1️⃣ 執行操作」
   ✅ 應看到綠色 ✓ 已新增個案

5. 點「2️⃣ 推送到雲端」
   ✅ 應看到綠色 ✅ 已推送到雲端
```

### 步驟 4：手機端打開測試工具
```
http://10.0.10.49:8000/sync-test.html

（將 10.0.10.49 替換為您的電腦 IP，見下方）
```

### 步驟 5：手機端配置
```
切換到 📱 手機端，填入：
• 同步碼：center-a（必須相同！）
• API：https://smart-care-sync-api.j9zrzt95b6.workers.dev/state
• 點「保存設定」
```

### 步驟 6：手機端驗證
```
1. 點「拉取最新資料」
2. 等待操作日誌顯示綠色訊息
3. 查看下方「手機本地資料」表格
4. 個案數應 > 0

✅ 看到 → 同步成功！
```

---

## 📍 獲取電腦 IP 地址

### Windows 用戶
```
1. 打開 CMD 或 PowerShell
2. 輸入：ipconfig
3. 查找「IPv4 位址」（例如 192.168.1.100）
4. 使用：http://192.168.1.100:8000/sync-test.html
```

### Mac/Linux 用戶
```
1. 打開終端
2. 輸入：hostname -I
3. 複製顯示的 IP（例如 10.0.10.49）
4. 使用：http://10.0.10.49:8000/sync-test.html
```

---

## ✅ 驗證檢查清單

### 生產環境驗證
- [ ] 電腦端頁面正常加載
- [ ] 工具列顯示「立即同步」按鈕
- [ ] 同步碼顯示 center-a
- [ ] 電腦端新增個案成功
- [ ] 點「立即同步」後顯示「已同步」
- [ ] 手機端頁面正常加載
- [ ] 手機端點「立即同步」
- [ ] **手機能看到電腦新增的個案** ✅ 成功！

### 測試工具驗證
- [ ] 電腦端測試工具打開
- [ ] 手機端測試工具打開（使用電腦 IP）
- [ ] 雙端同步碼相同（center-a）
- [ ] 電腦端執行操作 + 推送
- [ ] 手機端拉取資料
- [ ] **手機本地資料表顯示個案數 > 0** ✅ 成功！

---

## 🔍 實時監控（開發者工具）

### 打開開發者工具
```
F12 或 Ctrl+Shift+I（Windows）
Cmd+Option+I（Mac）
```

### Network 頁籤
```
1. 打開 Network 頁籤
2. 清空日誌
3. 點「立即同步」
4. 應看到：
   ✅ GET 請求 /state?key=center-a
   ✅ POST 請求 /state?key=center-a
   ✅ 狀態碼 200
```

### Application/Storage 頁籤
```
1. 打開 Application 頁籤
2. 展開 LocalStorage
3. 查看 smart-care-state-v1
4. 應包含 clients 數據
```

---

## ⚠️ 常見問題快速排查

### 問題 1：手機看不到電腦新增的資料

**可能原因 1：同步碼不同**
```
檢查：電腦和手機的同步碼是否完全相同？
解決：
  電腦：設定 → 同步碼 center-a
  手機：設定 → 同步碼 center-a（複製貼上，不要手打）
```

**可能原因 2：未按順序操作**
```
正確流程：
  1️⃣ 電腦新增個案
  2️⃣ 電腦立即同步
  3️⃣ 手機立即同步
  
不能跳過任何步驟！
```

**可能原因 3：API 無法連線**
```
檢查方式 (F12 → Console)：
  fetch('https://smart-care-sync-api.j9zrzt95b6.workers.dev/state?key=center-a')
    .then(r => r.json())
    .then(d => console.log(d))
    
應返回：{ok: true, value: {...}}
```

### 問題 2：頁面無法加載

**生產環境無法打開**
```
URL：https://j9zrzt95b6-sudo.github.io/shiny-journey/?syncKey=center-a

檢查：
  1. 網路連接是否正常
  2. 瀏覽器是否最新版本
  3. 試試清除瀏覽器快取（Ctrl+Shift+Delete）
```

**本地工具無法打開**
```
URL：http://localhost:8000/sync-test.html

檢查：
  1. 本地服務器是否運行中
  2. 是否使用了正確的 IP 地址
  3. 防火牆是否阻止了 8000 端口
```

### 問題 3：看到錯誤訊息

```
查看操作日誌中的錯誤訊息，常見的有：

❌ 「API 連線失敗」
   → 檢查 API 位址是否正確

❌ 「版本衝突」（409）
   → 兩端同時修改了同一資料
   → 自動選用最新版本

❌ 「未找到數據」
   → 雲端還沒收到電腦的資料
   → 確保電腦已點「立即同步」或「推送到雲端」
```

---

## 🎬 實時測試流程（邊操作邊查看）

### 第 1 分鐘：準備
```
□ 電腦打開應用（生產或測試工具）
□ 手機打開應用（同一工具）
□ 雙端配置相同的同步碼（center-a）
□ 打開 DevTools (F12)，Network 頁籤
```

### 第 2 分鐘：電腦操作
```
□ 新增個案「測試_${時間}」
□ 點「立即同步」或「推送到雲端」
□ 觀察 Network 中的請求
□ 應看到 POST /state?key=center-a 200
```

### 第 3 分鐘：手機驗證
```
□ 點「立即同步」或「拉取最新資料」
□ 觀察 Network 中的 GET 請求
□ 應看到 GET /state?key=center-a 200
□ 查看個案列表或資料表
□ 應看到電腦新增的個案！
```

---

## 📊 測試結果記錄

### 測試基本信息
```
測試日期：2026-07-26
同步碼：center-a
API：https://smart-care-sync-api.j9zrzt95b6.workers.dev/state
環境：□ 生產  □ 本地測試工具
```

### 測試步驟 1：新增個案
```
操作內容：新增個案 李小明
電腦端結果：
  □ 新增成功
  □ 立即同步成功
  時間：_______
  
手機端結果：
  □ 同步成功
  □ 看到新增個案
  時間：_______
```

### 測試步驟 2：修改個案
```
操作內容：修改個案備註
電腦端結果：
  □ 修改成功
  □ 立即同步成功
  
手機端結果：
  □ 同步成功
  □ 看到修改內容
```

### 總體結果
```
□ 所有測試通過 ✅ 同步功能正常
□ 部分失敗 ⚠️ 詳見下方
□ 全部失敗 ❌ 需要故障排除

失敗詳情：
_______________________________
```

---

## 🎯 建議測試次序

### 第一次測試（驗證基本功能）
```
1. 生產環境：電腦 + 手機，同步碼 center-a
2. 只測試新增個案
3. 不需要修改或刪除
4. 測試時間：5 分鐘
```

### 第二次測試（進階功能）
```
1. 本地測試工具：電腦 + 手機
2. 測試新增、修改、刪除
3. 測試多筆連續操作
4. 測試時間：15 分鐘
```

### 第三次測試（壓力測試）
```
1. 添加 10+ 筆數據
2. 同時修改多筆
3. 測試網路中斷恢復
4. 測試時間：20 分鐘
```

---

## ✨ 成功標誌

### 階段 1：基本同步 ✅
```
□ 電腦端能新增資料
□ 手機端能拉取到新資料
□ 同步耗時 < 5 秒
```

### 階段 2：完整同步 ✅
```
□ 能修改資料並同步
□ 能刪除資料並同步
□ 多筆數據無遺漏
```

### 階段 3：穩定同步 ✅
```
□ 多次同步無故障
□ 網路恢復後能自動同步
□ 重新整理後資料保留
```

---

## 🚀 立即開始

### 最快方案（1 分鐘準備）
```
1. 複製：https://j9zrzt95b6-sudo.github.io/shiny-journey/?syncKey=center-a
2. 電腦瀏覽器打開
3. 手機瀏覽器打開
4. 開始操作
```

### 詳細方案（2 分鐘準備）
```
1. 電腦打開：http://localhost:8000/sync-test.html
2. 手機打開：http://[電腦IP]:8000/sync-test.html
3. 雙端配置同步碼 center-a
4. 按步驟操作
```

---

**準備好了嗎？選擇上方方案之一，立即開始！** 🎉

時間：現在
地點：您的電腦和手機
工作：驗證跨裝置同步功能
預期時間：3-5 分鐘

**Go！** 🚀
