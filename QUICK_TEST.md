快速測試指南：電腦修改 → 手機同步驗證
===============================================

## 📋 建立的測試工具

1. **sync-test.html** ⭐ 推薦
   - 打開此文件即可開始測試
   - 支持同時在電腦和手機上打開
   - 視覺化介面，易於操作
   
2. **SYNC_TEST.md**
   - 詳細的測試步驟和故障排除指南
   - 包含所有測試場景說明

3. **sync-test.js**
   - 命令行自動化測試工具
   - 用於測試 API 層的同步機制

---

## 🚀 最快的驗證步驟 (5分鐘)

### 準備工作
□ 用電腦打開：http://localhost:8000/sync-test.html
□ 用手機打開：http://[電腦IP]:8000/sync-test.html
  （或掃描二維碼、複製連結）

### 電腦端操作
1. 確保切換到 💻 **電腦端** 模式
2. 設定同步碼（預設值即可）：test-sync-2026
3. 設定 API 位址（預設值即可）
4. 點擊「保存設定」
5. 在「測試資料描述」輸入：測試個案_小明
6. 點擊「執行操作」→ 看到 ✓ 綠色提示

### 手機端驗證
1. 確保切換到 📱 **手機端** 模式
2. 設定 **相同的同步碼和 API 位址**
3. 點擊「保存設定」
4. 點擊「拉取最新資料」
5. 查看下方表格：
   ✅ 如果看到「個案」記錄數 ≥ 1 → **同步成功**
   ❌ 如果還是 0 → **同步失敗**（見故障排除）

### 驗證結果
- 電腦「執行操作」會在本地 localStorage 增加資料
- 手機「拉取最新資料」會從 API 讀取電腦推送的資料
- 若手機成功看到新資料，則跨裝置同步 ✅ 正常

---

## 🔍 進階測試場景

### 場景 B：修改資料同步
1. 電腦端：點擊「查看快取內容」記下某個個案名稱
2. 電腦端：選擇「修改個案」，輸入新的修改內容，點「執行操作」
3. 手機端：點「拉取最新資料」
4. 手機端：點「對比電腦/手機資料」檢查修改是否同步

### 場景 C：連續多筆操作
1. 電腦端：連續執行多個「新增個案」操作
2. 手機端：點「拉取最新資料」一次
3. 驗證手機是否能看到所有新增的個案

### 場景 D：網路中斷恢復
1. 電腦端：執行操作後，打開 DevTools (F12) → Network → 設定 Offline
2. 手機端：嘗試拉取資料 → 應顯示 ❌ 連線失敗
3. 電腦端：Online
4. 手機端：再次拉取資料 → 應 ✅ 成功

---

## 🐛 常見問題排除

### 問題：手機拉取後還是 0 個案

**檢查清單：**
- [ ] 同步碼完全相同（複製貼上而不是手打）
- [ ] API 位址完全相同且無誤
- [ ] API 服務是否正常（先執行 `./scripts/cloudflare-healthcheck.sh`）
- [ ] 打開瀏覽器 DevTools → Network → 查看請求狀態

**操作步驟：**
1. 手機：F12 打開開發者工具
2. 切到 Network 頁籤
3. 點「拉取最新資料」
4. 查看請求的 URL 和回應：
   - 如果看到 `404` → API 位址錯誤
   - 如果看到 `400` → 同步碼格式錯誤
   - 如果看到 `500` → 服務器錯誤
   - 如果成功 `200` 但數據為空 → 電腦端未成功寫入

### 問題：電腦端顯示 ✓ 但手機看不到

**可能原因：**
- 同步碼不同
- API 位址不同
- 電腦端的操作沒有真正保存到 localStorage

**驗證方法：**
1. 電腦端：點「查看快取內容」→ 應看到個案數 > 0
2. 如果為 0，說明操作未保存 → 重新執行操作
3. 手機端打開 DevTools 檢查 localStorage 內容

### 問題：操作日誌中有警告 ⚠️

- `⚠ 請先切換到電腦端` → 點對應的裝置按鈕
- `⚠ 尚未設定 API 位址和同步碼` → 填寫設定並保存
- `⚠ 沒有可修改的個案` → 先執行「新增個案」

---

## 📊 驗收標準

以下各項都能 ✅ 通過，則表示同步正常：

- [ ] 電腦新增個案 → 手機能拉取
- [ ] 電腦修改個案 → 手機能看到更新
- [ ] 多筆資料同時同步 → 無遺漏
- [ ] 刷新手機後重新拉取 → 資料仍在
- [ ] API 連線失敗後恢復 → 能自動重新同步

---

## 💡 開發者技巧

在瀏覽器 Console (F12) 執行這些命令快速檢查：

```javascript
// 查看本機 localStorage 中的資料
JSON.parse(localStorage.getItem('smart-care-state-v1'))

// 查看同步狀態元信息
JSON.parse(localStorage.getItem('smart-care-state-v1'))._meta

// 清空本機資料（謹慎使用！）
localStorage.removeItem('smart-care-state-v1')

// 查看設定
JSON.parse(localStorage.getItem('sync-test-config'))
```

---

## 📞 無法解決的問題

如果按照以上步驟仍無法解決，請提供以下信息：

1. 電腦和手機的瀏覽器名稱及版本
2. 使用的同步碼和 API 位址
3. DevTools Network 中看到的完整請求和回應
4. 操作日誌中最後的 5 條記錄
5. 錯誤訊息的完整截圖

這些信息將幫助快速診斷問題。

